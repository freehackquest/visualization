#include "parseArgs.h"
#include <iostream>
#include <QString>

QStringList HelpersParseArgs::parse(QString str){
	int len = str.length();
	for(int i = 0; i < len; i++){
		
	}
	// TODO
	return str.split(" ");
};

